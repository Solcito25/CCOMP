#include <iostream>
using namespace std;
 int main(){
 cin>>a;
 float c;
 bool d = true;
 while(d== true){
    c=a/2
    if(c-a != 0.0)
        d
        cout<<"No es potencia de dos"
    else if (c==1)
        cout<<"No es potencia de dos"
    else
        d=true


 }
 return 0;


 }
